//! Reserved for a future richer terminal UI.
//!
//! The shipped interactive mode is the text-mode `dota>` shell implemented in
//! this module's parent (`tui::launch_tui`). This file is intentionally a stub:
//! an earlier revision advertised a `ratatui`-based UI that was never
//! implemented, so the `ratatui`/`crossterm` dependencies and the phantom
//! keybindings they implied were removed (SECURITY-AUDIT.md H1).
